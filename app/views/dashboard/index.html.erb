Hi user
<%= @user.username %>

<%= button_to "logout", logout_path, method: 'delete' %>

<% @golden_grams.each do |gram| %>
  <%= gram.images[:standard_resolution] %>
  <%= gram.caption[:text] %>
  <%= gram.tags %>
  <%= gram.likes.count %>
  <%= gram.users_in_photo %>
<% end %>
